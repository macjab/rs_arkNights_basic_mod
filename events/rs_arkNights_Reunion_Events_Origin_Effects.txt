namespace = reunion_events_origin_effects_control

### Hidden Game Start Event
event = {
	id = reunion_events_origin_effects_control.100
	hide_window = yes
	is_triggered_only = yes

	trigger = {
		has_origin = rs_arkNights_Reunion_Origin_Shoulders_Of_Giants
	}

	immediate = {
		owner = {
			capital_scope = {
				while = {
					limit = { 
						num_pops < 36
					}
					create_pop = {
						species = owner_main_species
						ethos = random
					}
				}

				add_district = district_mining
				add_district = district_mining
				add_district = district_mining
			}
		}
	}
}